import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Package and install a plugin","description":"","frontmatter":{"editSource":"docs/user/develop/basic/publish.md"},"headers":[],"relativePath":"en/develop/basic/publish.md","filePath":"en/develop/basic/publish.md"}');
const _sfc_main = { name: "en/develop/basic/publish.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="package-and-install-a-plugin" tabindex="-1">Package and install a plugin <a class="header-anchor" href="#package-and-install-a-plugin" aria-label="Permalink to &quot;Package and install a plugin&quot;">​</a></h1><p>The previous tutorials loaded a local plugin through a <code>--patch</code> overlay. This tutorial packages it as an installable <strong>bundle</strong>, installs it into a <strong>profile</strong> with <code>dsh plugin add</code>, and explains the layer order that determines the composed configuration. It assumes the <code>dsh</code> CLI is installed. Complete <a href="./config">plugin configuration</a> first.</p><p>To use a fresh source checkout instead, complete the <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/README.md#run-from-source" target="_blank" rel="noreferrer">run-from-source section</a>, keep this tutorial&#39;s <code>hello-plugin</code> directory at the repository root, and run the remaining <code>dsh ...</code> commands from there as <code>pnpm dsh ...</code>. See <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/cli/reference/README.md#source-execution" target="_blank" rel="noreferrer">source execution</a> for build and launcher behavior.</p><h2 id="two-concepts-two-manifests" tabindex="-1">Two concepts, two manifests <a class="header-anchor" href="#two-concepts-two-manifests" aria-label="Permalink to &quot;Two concepts, two manifests&quot;">​</a></h2><p>Installation is built on two concepts. Both are described by a <code>package.json</code>, but they carry different kinds of manifest under the <code>dsh</code> key, and they answer different questions:</p><ul><li>A <strong>bundle</strong> is an npm package that ships a configuration layer. Its manifest declares <code>dsh.bundle</code>, answering &quot;what does this package contribute?&quot;: a patch file that inserts or overrides plugin rows.</li><li>A <strong>profile</strong> is a directory under <code>$DSH_HOME/profiles/&lt;name&gt;</code> describing one runnable composition. Its manifest declares <code>dsh.profile</code>, answering &quot;which bundles compose this setup, in what order?&quot;.</li></ul><p>A bundle is what you author and distribute; a profile is what a user boots with <code>dsh --profile &lt;name&gt;</code>. Nothing is both.</p><h3 id="the-bundle-manifest" tabindex="-1">The bundle manifest <a class="header-anchor" href="#the-bundle-manifest" aria-label="Permalink to &quot;The bundle manifest&quot;">​</a></h3><p>Create the package directory:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -p</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hello-plugin</span></span></code></pre></div><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>hello-plugin/</span></span>
<span class="line"><span>├── package.json       # declares dsh.bundle</span></span>
<span class="line"><span>├── cordis.patch.yml   # the layer applied when a profile lists this bundle</span></span>
<span class="line"><span>└── index.js           # plugin modules the patch rows reference</span></span></code></pre></div><p>Create <code>hello-plugin/package.json</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;name&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;dsh-hello-plugin&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;version&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;0.1.0&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;type&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;module&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;main&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;index.js&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;files&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;index.js&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;cordis.patch.yml&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;dsh&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;bundle&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;patch&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;./cordis.patch.yml&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Create <code>hello-plugin/index.js</code> with the plugin entry point:</p><div class="language-js vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">js</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;hello-plugin&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> apply</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">() {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  console.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">log</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;[hello-plugin] plugin loaded!&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Create <code>hello-plugin/cordis.patch.yml</code>. The patch is a YAML array like the <code>--patch</code> overlays you have been writing, except plugin rows reference the package by name instead of a relative source path so Node resolution finds the installed code:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">- </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">insert</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    - </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">hello</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">      name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">dsh-hello-plugin</span></span></code></pre></div><p>A package without the <code>dsh.bundle</code> declaration still installs, but only as a plain dependency: <code>dsh plugin</code> prints a warning and activates no layer. Use that package format for a library that plugin packages import rather than a plugin users enable.</p><h3 id="the-profile-manifest" tabindex="-1">The profile manifest <a class="header-anchor" href="#the-profile-manifest" aria-label="Permalink to &quot;The profile manifest&quot;">​</a></h3><p>A profile directory holds two files:</p><ul><li><code>package.json</code> — the profile&#39;s out-of-tree plugin dependencies (managed by pnpm) plus the <code>dsh.profile</code> manifest with its ordered <code>bundles</code> list.</li><li><code>cordis.patch.yml</code> — the user&#39;s own patch layer, applied after every bundle layer.</li></ul><p>You never write a profile manifest by hand: <code>dsh plugin</code> creates and maintains it. The next section shows the result.</p><h2 id="install-into-a-profile" tabindex="-1">Install into a profile <a class="header-anchor" href="#install-into-a-profile" aria-label="Permalink to &quot;Install into a profile&quot;">​</a></h2><p><code>dsh plugin --profile &lt;name&gt; &lt;args...&gt;</code> forwards to pnpm in the profile directory, so every pnpm verb works. From the directory that contains <code>hello-plugin</code>, install the package checkout:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">dsh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --profile</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> demo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ./hello-plugin</span></span></code></pre></div><p>The first use initializes the profile (with <code>@deepseek-ai/dsh-base</code> as its first bundle), pnpm links the checkout, and <code>dsh</code> appends the bundle to <code>dsh.profile.bundles</code> because the package declares <code>dsh.bundle</code>:</p><div class="language-json vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;name&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;dsh-profile-demo&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;private&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;dependencies&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;dsh-hello-plugin&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;link:/path/to/hello-plugin&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;dsh&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;profile&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;bundles&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;@deepseek-ai/dsh-base&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">        &quot;dsh-hello-plugin&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      ]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Verify the layer without booting, then boot:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">dsh</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --profile</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> demo</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --dump-config</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">   # shows a &quot;# == dsh-hello-plugin&quot; layer</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">dsh</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --profile</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> demo</span></span></code></pre></div><p><code>dsh plugin --profile demo remove dsh-hello-plugin</code> removes both the dependency and the layer.</p><h2 id="the-loading-order" tabindex="-1">The loading order <a class="header-anchor" href="#the-loading-order" aria-label="Permalink to &quot;The loading order&quot;">​</a></h2><p>The effective configuration composes over an empty root by applying, in order:</p><ol><li>Each bundle patch named in the profile&#39;s <code>dsh.profile.bundles</code> list, in list order — <code>@deepseek-ai/dsh-base</code> first, then each installed bundle in the order it was added.</li><li>The profile&#39;s own <code>cordis.patch.yml</code>.</li><li>The home-level <code>$DSH_HOME/cordis.patch.yml</code> — machine-local preferences shared by every profile.</li><li>Each <code>--patch &lt;path&gt;</code> overlay, in argv order.</li></ol><p>App arguments are not another patch layer. A surface bundle can resolve them through an ordinary app-owned service, described below.</p><p>Later layers win per row, and a patch replaces a row&#39;s entire <code>config</code> value rather than deep-merging keys. Two consequences for bundle authors:</p><ul><li>Your patch can override rows from earlier layers by <code>id</code> — the same way <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/bundle/web-app/cordis.patch.yml" target="_blank" rel="noreferrer">the <code>dsh-web-app</code> bundle</a> overrides <code>dsh-base</code> rows — but must restate every key the row needs, not just the changed one.</li><li>Users can override your rows in their profile&#39;s <code>cordis.patch.yml</code> without touching your package, so prefer configuration defaults users are likely to keep and let the schema carry the rest.</li></ul><p>In-box bundle names always resolve from the dsh installation itself; pnpm manages only out-of-tree packages, so your bundle can rely on <code>@deepseek-ai/dsh-base</code> being present and current.</p><h2 id="give-a-surface-bundle-its-own-command-line" tabindex="-1">Give a surface bundle its own command line <a class="header-anchor" href="#give-a-surface-bundle-its-own-command-line" aria-label="Permalink to &quot;Give a surface bundle its own command line&quot;">​</a></h2><p>A bundle that defines a runnable app mounts an ordinary provider plugin:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">- </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">hello-startup</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;dsh-hello-plugin/startup&#39;</span></span></code></pre></div><p>The plugin exports <code>inject = [&#39;cmdlineArgs&#39;]</code>, calls <code>parseCmdline</code> from <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/boot/cmdline/README.md" target="_blank" rel="noreferrer"><code>@deepseek-ai/dsh-cmdline</code></a> with its own commander program, and provides its app-owned service from the program&#39;s action. The launcher hands every plugin the same immutable arguments after launcher flags, so app-specific flags need no launcher change and multiple plugins may parse the snapshot. The Loader row needs no launcher marker or special kind.</p><p>Rows configured by those arguments inject the provider&#39;s service and read it from their own <code>!!js</code> options, with the deployment value beside it as the fallback:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">- </span><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">id</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">my-app</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  name</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;@example/my-app&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  inject</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">myAppStartup</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  config</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">    port</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!!js</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ctx.myAppStartup.port ?? 8080</span></span></code></pre></div><p>On <code>--help</code>, the provider publishes no service, so those rows never activate. Loader mounts the composition once, waits for each row&#39;s ordinary injections, and only then evaluates that row&#39;s <code>!!js</code> config against its injected context.</p><h2 id="installing-from-github-the-build-script-catch" tabindex="-1">Installing from GitHub: the build-script catch <a class="header-anchor" href="#installing-from-github-the-build-script-catch" aria-label="Permalink to &quot;Installing from GitHub: the build-script catch&quot;">​</a></h2><p>Publishing to a registry is not required — users can install straight from a git host:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">dsh</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --profile</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> demo</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> add</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> github:you/hello-plugin</span></span></code></pre></div><p>But a git install fetches <strong>sources, not built artifacts</strong>: nothing runs your <code>build</code> script, so a TypeScript package arrives without its <code>lib/</code> output and fails to load. Two things must happen, one on each side:</p><ul><li><p><strong>The author</strong> ships a <code>prepare</code> script — pnpm runs it after a git install — that builds the published entry points from source, self-contained: it must not assume dev-only context such as a sibling monorepo checkout. <a href="https://github.com/deepseek-harness/turtle-ui" target="_blank" rel="noreferrer">turtle-ui</a> is a working example: its <code>prepare</code> runs a dedicated tsdown config that transpiles <code>src/</code> without project references or type checking.</p></li><li><p><strong>The user</strong> allowlists the build. pnpm ≥10 refuses to run a git dependency&#39;s <code>prepare</code> script until it is explicitly allowed, so the first <code>add</code> fails; <code>dsh</code> points at the fix — copy the exact package key pnpm printed into the profile&#39;s <code>pnpm-workspace.yaml</code>:</p><div class="language-yaml vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">yaml</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">allowBuilds</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#22863A", "--shiki-dark": "#85E89D" })}">  dsh-hello-plugin</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span></code></pre></div><p>and re-run the <code>add</code>.</p></li></ul><p>Treat that allowance as what it is: <strong>permission to execute the package&#39;s code on your machine at install time</strong>, outside any sandbox the agent runs under. Only allow packages whose source you trust, and pin a commit (<code>github:you/hello-plugin#&lt;sha&gt;</code>) so a later push cannot silently change what runs.</p><p>If you would rather not ask users for the allowance, distribute built artifacts instead — neither form needs any build permission:</p><ul><li><strong>Publish to npm</strong> with <code>lib/</code> built at <code>pnpm publish</code> time; <code>dsh plugin add your-package</code> then installs prebuilt code.</li><li><strong>Ship a tarball</strong> from <code>pnpm pack</code>; users run <code>dsh plugin add ./hello-plugin-0.1.0.tgz</code>.</li></ul><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to &quot;Next steps&quot;">​</a></h2><ul><li><a href="./../framework/">Plugins and lifecycle</a> — the full plugin lifecycle</li><li><a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/apps/cli/reference/README.md" target="_blank" rel="noreferrer">CLI behavior reference</a> — exact layer precedence, flags, and profile mechanics</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/develop/basic/publish.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const publish = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  publish as default
};
