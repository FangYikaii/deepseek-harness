import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"工具编写参考","description":"","frontmatter":{"editSource":"docs/cookbook/adding-a-tool.zh.md"},"headers":[],"relativePath":"reference/cookbook/adding-a-tool.md","filePath":"reference/cookbook/adding-a-tool.md"}');
const _sfc_main = { name: "reference/cookbook/adding-a-tool.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="工具编写参考" tabindex="-1">工具编写参考 <a class="header-anchor" href="#工具编写参考" aria-label="Permalink to &quot;工具编写参考&quot;">​</a></h1><p>面向模型的工具必须满足哪些约定，均以本文为准。如需按步骤构建第一个工具，请阅读<a href="./../../develop/basic/tool">构建工具</a>。<code>packages/shell/tool-bash</code> 是生产级的三包示例。</p><h2 id="最小形态" tabindex="-1">最小形态 <a class="header-anchor" href="#最小形态" aria-label="Permalink to &quot;最小形态&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { readFile } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;node:fs/promises&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { Context } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@deepseek-ai/cordis&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { defineTool } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@deepseek-ai/dsh-tools&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;my-tool&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> inject</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;tools&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> apply</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Context</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ctx.tools.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">register</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineTool</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    name: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;read_file&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Read a file from disk.&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// what the model sees</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    parameters: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      path: { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;string&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, required: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Absolute path&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      limit: { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;number&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },                     </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// optional by default</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    output: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      schema: { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;string&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">      render</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">_args</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">value</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [{ type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;text&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, text: value }],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    async</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> execute</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">args</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      // args is TYPED from the schema: { path: string; limit?: number }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      // exec carries immutable identity + token; signal is the operational field</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">      return</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> readFile</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(args.path, { encoding: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;utf8&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, signal: exec.signal })</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }))</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>注册基于副作用：dispose（资源释放）插件 fiber 即注销该工具。schema 会自动流入系统提示词的组装过程。</p><h2 id="execute-约定的规则" tabindex="-1">execute() 约定的规则 <a class="header-anchor" href="#execute-约定的规则" aria-label="Permalink to &quot;execute() 约定的规则&quot;">​</a></h2><ul><li><strong>参数已为你校验。</strong> <code>defineTool</code> 在 <code>execute</code> 运行前，会根据统一的 <code>ParameterSchemaSpec</code> 校验模型生成的 <code>arguments</code>（类型、必填键、字面量约束、恰好匹配一个分支的联合以及嵌套值——见<a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/architecture/2026-06-11-runtime-arg-validation.md" target="_blank" rel="noreferrer">运行时参数校验</a>），因此 <code>execute</code> 内的 args 会匹配 <code>InferArgs</code>。显式对象节点必须声明 <code>additionalProperties: true | false</code>；隐式参数根对象保持开放。你仍需手动检查 schema DSL 无法表达的约束，例如非空字符串、正数或跨字段规则。直接注册的原始 JSON Schema 工具自行负责输入校验。</li><li><strong>注册借用你的只读定义。</strong> 类型化的同进程贡献不是序列化边界；注册后不要修改其 schema 或替换回调。<code>schemas()</code> 只物化显式的模型可见投影。如需热替换工具，请 dispose 其所属副作用并注册替代品；回调闭包内的可变状态仍是普通的插件状态。</li><li><strong>执行身份受保护。</strong> 注册表在一次递归遍历中将 <code>arguments</code> 物化为分离的无损 JSON，在策略开始前冻结该值，并分配一个不透明的 <code>exec.token</code>；<code>callId</code>、<code>name</code>、<code>arguments</code>、<code>agent</code>、<code>token</code>、必填且由调用方持有的 <code>signal</code>，以及可选的外层传输 <code>parent</code> token 在整个分发过程中保持不可变。<code>parent</code> 仅用于身份标识，不暴露活跃的外层执行。请将 <code>args</code> 视为只读输入。只有 around-dispatch 包装器会收到可变视图；它可以替换并恢复必填的 <code>exec.signal</code> 以施加截止时间，但不能移除该信号。</li><li><strong>声明并返回一个规范 JSON 值。</strong> <code>output.schema</code> 使用 <code>ValueSchemaSpec</code>，根可以是对象、数组、标量或 null。<code>execute</code> 只返回推导出的值；注册表将其快照为无损 JSON，完成校验和冻结后，再传给 <code>output.render(args, value)</code>。工具主体不要返回内容块，也不要迫使调用方从自然语言中解析 id 和字段。</li><li><strong>抛出异常或返回无效值意味着 <code>isError</code>。</strong> 注册表会捕获异常，并在观察者运行前收敛 schema、渲染器、元数据投影器和无损 JSON 失败。基础设施故障请抛异常。成功的领域结果即使表示不理想的状态，也应写入规范值；其 Native 渲染器可以解释该状态，例如进程以非零状态退出。</li><li><strong>遵守 <code>exec.signal</code>。</strong> 信号触发时取消进行中的工作。</li><li><strong>使用 <code>presentationMeta</code> 投影持久化的卡片数据（可选）。</strong> <code>output.presentationMeta(args, value)</code> 从同一个规范值派生可回放的 JSON。核心将其持久化在 <code>tool/result</code> 上并传给 <code>presentResult</code>，因此需要结果期事实的卡片——例如 <code>write</code>／<code>edit</code> 的已应用 hunk——无需持久化规范值也能在回放中重现。嵌套 Code 分发没有卡片，因此会跳过该投影器。</li><li><strong>使用 <code>exec.agent</code> 发送异步通知。</strong> <code>agent.inject({ content, source: { kind: &#39;plugin&#39;, plugin: &#39;&lt;name&gt;&#39; } })</code> 追加持久化上下文，下一次模型请求会看到它——这不是唤醒（空闲的 agent（智能体）保持空闲）。请防范已 dispose 的 agent（try/catch）。</li></ul><h2 id="长时间运行的工作" tabindex="-1">长时间运行的工作 <a class="header-anchor" href="#长时间运行的工作" aria-label="Permalink to &quot;长时间运行的工作&quot;">​</a></h2><p>通过 producer 配置控制 <code>run_in_background</code>，然后使用 <code>ctx.jobs.start({ kind, label, owner: exec.agent, run })</code> 注册任务。注册表会在进入 producer 主体前将已预先中止的调用判为失败；运行时会在 <code>run()</code> 启动工作前校验 owner 和任务控制器是否可用，随后提供 id、会话围栏、通用控制工具、通知和 owner cleanup。成功的后台分支会返回类型化的规范句柄，如 <code>{ kind: &#39;background&#39;, jobId }</code>；其 Native 渲染器可以保留 <code>started background job bash-1</code> 这类供人阅读的自然语言，但 Code Mode 绝不能通过解析该文本取得 id。</p><p>producer 提供同步的 <code>cancel</code>、在资源清理后 settle 且不 reject 的 <code>done</code>，以及可选的消费式 <code>readOutput</code>（负责有界输出的格式化）。预先中止的调用属于失败，因为此时没有任务，其 id 无法满足成功输出 schema。<code>ctx.jobs.start()</code> 发布 id 后，应使用任务自有的取消信号，而不是 <code>exec.signal</code>：之后取消外层调用只会停止等待本次调用，不会终止已经发布的工作；该生命周期归 <code>job_kill</code>、owner dispose 和服务 teardown 所有。前台工作仍与 <code>exec.signal</code> 耦合。流式 producer 的示例和完整约定见<a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/architecture/2026-06-20-generic-long-running-tool-runtime.md" target="_blank" rel="noreferrer">后台任务运行时 Agent Note</a>与 <code>dsh-tool-bash</code>。</p><p><a id="execution-policy-and-observation"></a></p><h2 id="执行策略与观测" tabindex="-1">执行策略与观测 <a class="header-anchor" href="#执行策略与观测" aria-label="Permalink to &quot;执行策略与观测&quot;">​</a></h2><p>尽量不要把部署策略内建到工具中。使用 <code>tools/pre-execute</code> 实现可扩展的允许／拒绝／询问策略（见<a href="./extension-cookbook#a-hook-plugin-permission-gate-example">权限门禁示例</a>）；使用 <code>ctx.tools.guard()</code> 设置最终的单调拒绝，后续监听器无法撤销；使用 <code>tools/execute</code> 为分发添加截止时间、重试或指标收集；使用 <code>tools/post-execute</code> 替换展示内容或返回值、阻止结果，或附加模型可见上下文；使用 <code>tools/result</code> 观测不可变的归一化结果而不改变它。替换内容不会阻止程序化访问 <code>value</code>；保密策略会屏蔽或替换该值。沙箱实现也可以在工具的执行器实现中运行；<a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/core/tools/README.md#extension-points" target="_blank" rel="noreferrer"><code>dsh-tools</code> README</a> 定义每个扩展点的输入、顺序、返回值和失败行为。</p><h2 id="code-mode-自动触达你的工具" tabindex="-1">Code Mode 自动触达你的工具 <a class="header-anchor" href="#code-mode-自动触达你的工具" aria-label="Permalink to &quot;Code Mode 自动触达你的工具&quot;">​</a></h2><p>在 <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/core/tools/README.md" target="_blank" rel="noreferrer">Code Mode</a> 中，每个可见的已注册工具都可通过 <code>await tools.&lt;name&gt;(args)</code> 调用，无需额外集成。生成的 <code>ToolArgsMap</code> 和 <code>ToolOutputMap</code> 会根据同一组 schema 分别派生精确的参数类型与规范返回类型，调用则重新进入正常的执行流水线。成功调用会解析为策略处理后的最终规范 JSON 值，而不是渲染后的 Native 内容。失败调用会以真正的 <code>ToolCallError</code> reject；程序只能检查其 <code>name</code>、<code>toolName</code> 和可供人阅读的 <code>message</code>，无法取得内部错误代码或失败联合。</p><p>请把 <code>output.schema</code> 设计为实用的程序化 API：直接返回句柄与字段；当标量、数组或 null 确实就是结果时，允许采用相应的根类型；将面向人类的解释放入 <code>output.render</code>。中间值只存在于执行期间，不会被持久化或按提示词上限截断，也不设字节上限，因此生产方如实声明的采集边界和进程内存仍然重要。只有外层 <code>run_code</code> 日志／结果会受到可配置输出上限和面向模型的 spill 流水线约束。</p><h2 id="工具在-ui-中的渲染方式" tabindex="-1">工具在 UI 中的渲染方式 <a class="header-anchor" href="#工具在-ui-中的渲染方式" aria-label="Permalink to &quot;工具在 UI 中的渲染方式&quot;">​</a></h2><p>工具的 <code>output.render</code> 返回模型可见的内容；其 <strong>UI 卡片</strong> 是另一项独立关注点，通过纯展示投影以及可选的 <code>presentCall</code>／<code>presentResult</code> 方法声明。请将这些内容与规范值一并设计。没有 UI 展示方法的工具会回退到通用卡片（标题 = 工具名，原始 args 作为输入）。</p><p>两个方法都返回一个 <strong><code>card</code> 标签的渲染意图</strong>——选择与你的工具行为匹配的卡片类型：</p><ul><li><code>presentCall(args)</code> → 一个 <code>ToolCallView</code>（PENDING 卡片）： <ul><li><code>{ card: &#39;generic&#39;, title, kind?, rawInput?, content?, locations? }</code>——默认。设置 <code>kind</code> 获取图标（<code>read</code>／<code>search</code>／…）；设置 <code>locations: [{ path, line? }]</code> 标注工具涉及的文件，使有能力的编辑器跟随／跳转。</li><li><code>{ card: &#39;terminal&#39;, title, description?, cwd? }</code>——你的调用本身就是 shell 命令。<code>title</code> 是命令，<code>description</code> 渲染在终端卡片上方。（tool-bash。）</li><li><code>{ card: &#39;diff&#39;, title, diffs, locations? }</code>——你的调用创建或修改文件。<code>diffs: [{ path, oldText, newText }]</code>（新文件时 <code>oldText: null</code>）渲染为内联 diff 卡片。（tool-fs <code>write</code>／<code>edit</code>。）</li></ul></li><li><code>presentResult(args, { content, isError, meta? })</code> 返回完成后的卡片： <ul><li><code>generic</code> 提供可选的标题和内容。</li><li><code>terminal</code> 提供原始输出和可选的退出元数据；各 UI 根据自身能力渲染对应视图或回退视图。</li><li><code>diff</code> 提供已应用的 hunk，通常由 <code>output.presentationMeta</code> 派生并通过持久化的 <code>result.meta</code> 携带，使回放能重现它们。变更类工具保留 diff 结果，因为完成后的视图会替换 pending 卡片。</li><li><code>search</code> 提供从持久化 <code>result.meta</code> 重建的发现型结果：按文件分组的匹配（<code>shape: &#39;matches&#39;</code>，grep）或扁平路径列表（<code>shape: &#39;paths&#39;</code>，glob），外加 <code>truncated</code>／<code>total</code> 使 UI 永不把被截断的结果当作完整结果呈现。该视图不携带结果文本（无 search 卡片的 UI 回退到原始结果内容），也没有 <code>search</code> 调用视图——发现型调用的 pending 状态保持为 generic 卡片，因为匹配只在 <code>execute</code> 之后才存在。（tool-fs-search 的 <code>grep</code>／<code>glob</code>。）</li><li><code>web</code> 提供已完成的 web 检索，以 <code>kind: &#39;search&#39; | &#39;fetch&#39;</code> 区分（结构化的搜索来源或抓取摘要），由 <code>result.meta</code> 派生；它不携带正文副本，因此不具备 <code>web</code> 能力的 UI 回退到原始结果内容。（tool-web <code>web_search</code>／<code>web_fetch</code>。）</li></ul></li></ul><p>硬性规则（违反会出问题）：</p><ul><li><strong>纯函数。</strong> 这些方法在实时流式输出和会话日志回放时都会运行，因此必须是 <code>args</code>（加 result）的纯函数——不做 I/O、不读会话状态、不用时钟／随机数。diff 从 args 派生（<code>write</code> 使用 <code>oldText: null</code>，因为调用时的展示器没有文件先前内容）；会话上下文由 UI 适配器而非工具提供。如果你发现自己想在 <code>presentCall</code> 内获取文件旧内容或工作目录，请停下：那属于持久结果元数据或适配器，不属于展示器。</li><li><strong>UI 格式不进入模型结果。</strong> 围栏 <code>\`\`\`console</code> 块、diff、相对化路径均不应仅为服务 UI 而进入规范值或 Native 内容。<code>output.render</code> 负责模型可见的自然语言；<code>presentationMeta</code> 和卡片展示器负责可回放的 UI 状态。<code>terminal</code> 结果视图携带原始输出，由适配器按需添加回退格式。</li><li><strong><code>defineTool</code> 对展示路径做软校验。</strong> 格式错误或旧版日志中的参数会使包装器返回 <code>undefined</code>（通用回退）而非抛异常——展示绝不能导致回放崩溃。</li></ul><p>中性词汇定义在 <code>dsh-tools</code> 中；工具绝不导入 UI 或传输类型。host/client 运行时将每个 <code>card</code> 映射到各自的视图。设计与原因见<a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/architecture/2026-07-02-tool-render-intent-union.md" target="_blank" rel="noreferrer">渲染意图联合体 Agent Note</a>；<code>dsh-tool-fs</code>（generic/diff）和 <code>dsh-tool-bash</code>（terminal）是参考实现。</p><h2 id="验证" tabindex="-1">验证 <a class="header-anchor" href="#验证" aria-label="Permalink to &quot;验证&quot;">​</a></h2><p>遵循<a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/testing.md" target="_blank" rel="noreferrer">仓库测试策略</a>和所属包的测试文档。已交付且面向模型或 UI 的变更必须提供其中规定的组装覆盖。</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("reference/cookbook/adding-a-tool.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const addingATool = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  addingATool as default
};
