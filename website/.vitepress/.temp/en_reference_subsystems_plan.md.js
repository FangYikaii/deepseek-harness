import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Plan Mode","description":"","frontmatter":{"editSource":"docs/subsystems/plan.md","outline":[2,3]},"headers":[],"relativePath":"en/reference/subsystems/plan.md","filePath":"en/reference/subsystems/plan.md"}');
const _sfc_main = { name: "en/reference/subsystems/plan.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="plan-mode" tabindex="-1">Plan Mode <a class="header-anchor" href="#plan-mode" aria-label="Permalink to &quot;Plan Mode&quot;">​</a></h1><p>Plan mode is logged per-agent collaboration state owned by <a href="https://github.com/deepseek-ai/deepseek-harness/tree/master/packages/plan/plan-mode" target="_blank" rel="noreferrer">dsh-plan-mode</a> (<code>ctx.planMode</code>, <code>PlanModeController</code>): while active, a deployment-owned guidance section is included in each model request. Plan mode is <strong>soft guidance</strong>. <a href="./sandbox">Sandbox mode</a> and <a href="./approval">approval policy</a> enforce restrictions independently; neither reads or writes plan state, so deployments configure them separately. The package is optional, and the agent loop does not depend on it. It contributes the <code>plan:policy</code> prompt section and registers the <code>exit_plan_mode</code> tool and <code>/plan</code> command. The <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/simplification/2026-07-22-plan-specific-collaboration-state.md" target="_blank" rel="noreferrer">design note</a> owns the rationale; the <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/plan/plan-mode/README.md" target="_blank" rel="noreferrer">package README</a> owns the model-experience and limitation detail.</p><p>Source: <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/plan/plan-mode/src/index.ts" target="_blank" rel="noreferrer"><code>packages/plan/plan-mode/src/index.ts</code></a></p><h2 id="logged-state-and-recovery" tabindex="-1">Logged state and recovery <a class="header-anchor" href="#logged-state-and-recovery" aria-label="Permalink to &quot;Logged state and recovery&quot;">​</a></h2><p><code>plan/mode</code> (<code>{ active: boolean }</code>) is a log-only, whole-value-replace <a href="./session">session event</a>: durable and replayable, never in the model transcript. <code>foldPlanMode(events, end?)</code> returns the last logged value in the prefix, or <code>false</code> when there is none — the state in force is always a pure fold of the session log, so resume, fork, and compaction recover it with no live mirror, and UIs observe committed flips through <code>session/event</code>. The complete event declaration is in the <a href="./../persistence-catalog">persistence log event catalog</a>.</p><h2 id="pending-selections-and-the-pre-step-append" tabindex="-1">Pending selections and the pre-step append <a class="header-anchor" href="#pending-selections-and-the-pre-step-append" aria-label="Permalink to &quot;Pending selections and the pre-step append&quot;">​</a></h2><p>Because every session event is turn-enclosed, a user selection remains pending until the next accepted in-turn pre-step appends it before request derivation, in whichever turn that occurs. A selection never forces continuation, so one made after a turn&#39;s final accepted pre-step is appended in a later turn. <code>set(agent, active)</code> records the pending selection (a no-op when the target equals the logged-or-already-pending state), and <code>get(agent)</code> returns <code>{ active: boolean; pending?: boolean }</code>: the logged state used to assemble the current step plus the selected state waiting to be appended.</p><p>The only append point while an agent is running is a prepended <code>agent/pre-step</code> listener. It observes every proposed request step, including turn 1 step 1 and request-recovery retries, calls downstream listeners first, and appends only after they accept the step. Prompt admission happens before a turn and cannot append <code>plan/mode</code>, so a selection made at the prompt is appended by the first accepted in-turn pre-step of the turn it starts. An append failure cannot block the turn, and the selection remains pending for a later accepted in-turn pre-step. An appended user selection also records one plugin-sourced <code>user/message</code> notice, but only when the last logged request header described the other state, so the model is told exactly when its context changed and never redundantly. A selection made after a turn&#39;s final accepted pre-step remains process-local and is lost if the process exits before another accepted in-turn pre-step (<a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/plan/plan-mode/README.md#known-limitations-and-deferred-work" target="_blank" rel="noreferrer">README limitation</a>).</p><h2 id="configuration" tabindex="-1">Configuration <a class="header-anchor" href="#configuration" aria-label="Permalink to &quot;Configuration&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/** Deployment-owned plan guidance. */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">interface</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> PlanModeConfig</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">  /** Guidance rendered as the \`plan:policy\` prompt section while plan mode is active. */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">  section</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> string</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>A missing, blank, or non-string <code>section</code> and any unknown key fail at plugin load rather than being ignored. While plan mode is active, the exact <code>section</code> text renders as the <code>plan:policy</code> <a href="./system-prompt">system-prompt section</a> at order 50; inactive plan mode contributes no text.</p><h2 id="the-exit-tool-and-the-plan-command" tabindex="-1">The exit tool and the <code>/plan</code> command <a class="header-anchor" href="#the-exit-tool-and-the-plan-command" aria-label="Permalink to &quot;The exit tool and the \`/plan\` command&quot;">​</a></h2><p><a href="./../tool-catalog#deepseek-aidsh-plan-mode"><code>exit_plan_mode</code></a> stays registered while plan mode is inactive, so entering or leaving plan mode changes only the prompt section, never the request tool catalog; execution outside plan mode fails. In plan mode it requires a complete markdown plan starting with a <code>#</code> heading and presents it for review through the <a href="./user-questions">user-questions seam</a>. Approval returns <code>{ approved: true }</code> and records a silent (non-narrated) pending exit that is appended at the next accepted in-turn pre-step. Plan guidance therefore remains active for the rest of the assistant&#39;s current tool batch, and the tool result itself reports the transition. Keep-planning is a failed call carrying the user&#39;s feedback, so the model revises and presents again; a missing interaction channel and a service reload during review also fail the call rather than silently leaving plan mode.</p><p>When <a href="./commands"><code>ctx.commands</code></a> is composed, the plugin registers <code>/plan [off|message]</code>: bare <code>/plan</code> selects plan mode, any other non-empty message selects it and then submits the text through <code>agent.steer()</code> so it becomes the next step&#39;s ordinary logged user message under plan guidance, and the exact argument <code>off</code> selects inactive, which also cancels a pending entry before it is appended and becomes visible to a request.</p><h2 id="the-service" tabindex="-1">The service <a class="header-anchor" href="#the-service" aria-label="Permalink to &quot;The service&quot;">​</a></h2><p><code>ctx.planMode</code> owns the logged plan state, applies and narrates selected state at step start, and owns the <code>plan:policy</code> section, the <code>/plan</code> command, and the stable exit tool; <code>get</code>/<code>set</code> signatures are in the generated <a href="#ctxplanmode--planmodecontroller">service catalog</a>.</p><p><a id="cordis-surface"></a></p><h2 id="cordis-api" tabindex="-1">Cordis API <a class="header-anchor" href="#cordis-api" aria-label="Permalink to &quot;Cordis API&quot;">​</a></h2><p>Generated from source by <code>scripts/gen-cordis-catalog.ts</code> (verified fresh by <code>pnpm run verify-cordis-catalog</code> in doc-sync; regenerate with <code>pnpm run gen-cordis-catalog</code>) — this section is byte-identical in both language sides of the page. Signature blocks use a <code>ts cordis-catalog</code> fence and keep the original source JSDoc; dispatch modes are defined in the <a href="./../cordis-primer#dispatch-modes">primer</a>, and the framework-inherited <code>ctx</code> API lives in <a href="./../cordis-api/inherited">cordis-api/inherited.md</a>.</p><p><a id="ctxplanmode--planmodecontroller"></a></p><h3 id="ctx-planmode-—-planmodecontroller" tabindex="-1"><code>ctx.planMode</code> — <code>PlanModeController</code> <a class="header-anchor" href="#ctx-planmode-—-planmodecontroller" aria-label="Permalink to &quot;\`ctx.planMode\` — \`PlanModeController\`&quot;">​</a></h3><p><code>ctx.planMode</code>: owns logged plan state, applies and narrates selected state at step start, the <code>plan:policy</code> section, the <code>/plan</code> command, and the stable exit tool. UIs observe committed flips through <code>session/event</code>; there is no live mirror.</p><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/**</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * Read the logged plan state and any selected state awaiting the next</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * accepted in-turn pre-step.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> *</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">@param</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> agent</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> The agent to read.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">@returns</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> Current logged state plus a pending selection, when present.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">get</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(agent: Agent): { active: boolean; pending</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> boolean }</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">/**</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * Select whether plan mode should be active. Between turns the method</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * appends the change immediately because no in-turn pre-step will run until</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * another prompt starts a turn. The open-turn fold is the idle signal:</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * agent status stays \`running\` through post-turn checkpointing, when no</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * further in-turn pre-step runs. During an open turn the selection remains</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * pending until the next accepted in-turn pre-step. Repeated selection of</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * the current or already-pending state is a no-op.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> *</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">@param</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> agent</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> The agent to switch.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">@param</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> active</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> Whether plan mode should be active.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">@returns</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> what happened: \`committed\` (logged now), \`queued\` (awaiting the</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * next accepted in-turn pre-step), \`cancelled\` (an opposite pending selection</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * was cleared; the logged state already matches), or \`noop\` (already in that</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> * state).</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"> */</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">set</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(agent: Agent, active: boolean): </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;committed&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;queued&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;cancelled&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> |</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;noop&#39;</span></span></code></pre></div><p>Types: <a href="./core">Agent</a></p><p>Source: <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/plan/plan-mode/src/index.ts" target="_blank" rel="noreferrer"><code>packages/plan/plan-mode/src/index.ts:184</code></a></p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/reference/subsystems/plan.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const plan = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  plan as default
};
