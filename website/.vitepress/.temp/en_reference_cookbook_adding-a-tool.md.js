import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Tool authoring reference","description":"","frontmatter":{"editSource":"docs/cookbook/adding-a-tool.md"},"headers":[],"relativePath":"en/reference/cookbook/adding-a-tool.md","filePath":"en/reference/cookbook/adding-a-tool.md"}');
const _sfc_main = { name: "en/reference/cookbook/adding-a-tool.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="tool-authoring-reference" tabindex="-1">Tool authoring reference <a class="header-anchor" href="#tool-authoring-reference" aria-label="Permalink to &quot;Tool authoring reference&quot;">​</a></h1><p>Reference for the contracts a model-facing tool must satisfy. For an ordered first tool, follow <a href="./../../develop/basic/tool">Build a tool</a>. <code>packages/shell/tool-bash</code> is the production-grade three-package example.</p><h2 id="the-minimal-shape" tabindex="-1">The minimal shape <a class="header-anchor" href="#the-minimal-shape" aria-label="Permalink to &quot;The minimal shape&quot;">​</a></h2><div class="language-ts vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { readFile } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;node:fs/promises&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> type</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { Context } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@deepseek-ai/cordis&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { defineTool } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@deepseek-ai/dsh-tools&#39;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> name</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;my-tool&#39;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> inject</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;tools&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> function</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> apply</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">ctx</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">:</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> Context</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  ctx.tools.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">register</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">defineTool</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    name: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;read_file&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Read a file from disk.&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// what the model sees</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    parameters: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      path: { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;string&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, required: </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, description: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;Absolute path&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      limit: { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;number&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },                     </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// optional by default</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    output: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">      schema: { type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;string&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">      render</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: (</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">_args</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">value</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> [{ type: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;text&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, text: value }],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    async</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> execute</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(</span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">args</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">exec</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">) {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      // args is TYPED from the schema: { path: string; limit?: number }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">      // exec carries immutable identity + token; signal is the operational field</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">      return</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> readFile</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(args.path, { encoding: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;utf8&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, signal: exec.signal })</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }))</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>Registration is effect-based: disposing the plugin fiber unregisters the tool. Schemas flow into the system-prompt assembly automatically.</p><h2 id="rules-of-the-execute-contract" tabindex="-1">Rules of the execute() contract <a class="header-anchor" href="#rules-of-the-execute-contract" aria-label="Permalink to &quot;Rules of the execute() contract&quot;">​</a></h2><ul><li><strong>Args are validated for you.</strong> <code>defineTool</code> validates model-generated <code>arguments</code> against the unified <code>ParameterSchemaSpec</code> before <code>execute</code> runs (types, required keys, literal constraints, exact-one unions, and nested values — <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/architecture/2026-06-11-runtime-arg-validation.md" target="_blank" rel="noreferrer">runtime arg validation</a>), so inside <code>execute</code> the args match <code>InferArgs</code>. Explicit object nodes declare <code>additionalProperties: true | false</code>; the implicit parameter root stays open. You still hand-check constraints the DSL does not express, such as non-empty strings, positive numbers, or cross-field rules. Raw JSON-Schema tools registered directly own their input validation.</li><li><strong>Registration borrows your readonly definition.</strong> A typed same-process contribution is not a serialization boundary; do not mutate its schema or replace callbacks after registration. <code>schemas()</code> materializes only the explicit model-facing projection. To hot-swap a tool, dispose its owning effect and register the replacement; mutable state inside the callback&#39;s closure remains ordinary plugin state.</li><li><strong>Execution identity is protected.</strong> The registry materializes <code>arguments</code> as detached lossless JSON in one recursive pass, freezes that value before policy starts, and assigns an opaque <code>exec.token</code>; <code>callId</code>, <code>name</code>, <code>arguments</code>, <code>agent</code>, <code>token</code>, the required caller-owned <code>signal</code>, and an optional enclosing-transport <code>parent</code> token stay immutable through dispatch. <code>parent</code> is identity-only and exposes no live outer execution. Treat <code>args</code> as readonly input. Only an around-dispatch wrapper receives a mutable view, and it may replace and restore the required <code>exec.signal</code> to impose a deadline but cannot remove it.</li><li><strong>Declare and return one canonical JSON value.</strong> <code>output.schema</code> uses <code>ValueSchemaSpec</code> and may have an object, array, scalar, or null root. <code>execute</code> returns only the inferred value; the registry snapshots it as lossless JSON, validates it, freezes it, and passes it to <code>output.render(args, value)</code>. Do not return content blocks from the body or make callers parse prose for ids and fields.</li><li><strong>Throwing or returning an invalid value means <code>isError</code>.</strong> The registry catches throws and contains schema, renderer, metadata-projector, and lossless-JSON failures before observers run. Throw for infrastructure failures. Represent a successful domain outcome in the canonical value even when its Native renderer explains a non-ideal state, such as a non-zero process exit.</li><li><strong>Honor <code>exec.signal</code>.</strong> Cancel in-flight work when it fires.</li><li><strong>Project durable card data with <code>presentationMeta</code> (optional).</strong> <code>output.presentationMeta(args, value)</code> derives replayable JSON from the same canonical value. The core persists it on <code>tool/result</code> and hands it to <code>presentResult</code>, so a card that needs result-time facts—such as <code>write</code>/<code>edit</code> applied hunks—survives replay without persisting the canonical value. The projector is skipped for nested Code dispatches because they have no cards.</li><li><strong>Use <code>exec.agent</code> for async notifications.</strong> <code>agent.inject({ content, source: { kind: &#39;plugin&#39;, plugin: &#39;&lt;name&gt;&#39; } })</code> appends durable context the NEXT model request sees — it is not a wake-up (an idle agent stays idle). Guard against disposed agents (try/catch).</li></ul><h2 id="long-running-work" tabindex="-1">Long-running work <a class="header-anchor" href="#long-running-work" aria-label="Permalink to &quot;Long-running work&quot;">​</a></h2><p>Gate <code>run_in_background</code> with producer config, then register through <code>ctx.jobs.start({ kind, label, owner: exec.agent, run })</code>. The registry rejects a pre-aborted invocation before the producer body; the runtime validates ownership and task-controller availability before <code>run()</code> starts work, then supplies the id, session fence, generic control tools, notices, and owner cleanup. A successful background branch returns a typed canonical handle such as <code>{ kind: &#39;background&#39;, jobId }</code>; its Native renderer may keep human prose such as <code>started background job bash-1</code>, but Code Mode must never parse that prose to recover the id.</p><p>The producer supplies synchronous <code>cancel</code>, non-rejecting <code>done</code> that settles after resource cleanup, and optional consuming <code>readOutput</code> with bounded-output formatting. A pre-aborted call is a failure because no task exists whose id could satisfy the successful output schema. Once <code>ctx.jobs.start()</code> publishes the id, use a task-owned cancellation signal rather than <code>exec.signal</code>: later outer-call cancellation stops waiting for the call but does not kill published work; <code>job_kill</code>, owner disposal, and service teardown own that lifetime. Foreground work remains coupled to <code>exec.signal</code>. See the <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/architecture/2026-06-20-generic-long-running-tool-runtime.md" target="_blank" rel="noreferrer">background job runtime Agent Note</a> and <code>dsh-tool-bash</code> for a stream producer.</p><h2 id="execution-policy-and-observation" tabindex="-1">Execution policy and observation <a class="header-anchor" href="#execution-policy-and-observation" aria-label="Permalink to &quot;Execution policy and observation&quot;">​</a></h2><p>Prefer not to build deployment policy into the tool. Use <code>tools/pre-execute</code> for extensible allow/deny/ask policy (the <a href="./extension-cookbook#a-hook-plugin-permission-gate-example">permission-gate example</a>), <code>ctx.tools.guard()</code> for a final monotonic deny that later listeners cannot undo, <code>tools/execute</code> to wrap dispatch with a deadline, retry, or metrics collection, <code>tools/post-execute</code> to replace presentation content or the returned value, block the result, or attach model-facing context, and <code>tools/result</code> to observe the immutable normalized outcome. A content replacement leaves programmatic access to <code>value</code> intact; confidentiality policy blocks or replaces the value. A sandboxing implementation can also run inside the tool&#39;s executor implementation; the <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/core/tools/README.md#extension-points" target="_blank" rel="noreferrer"><code>dsh-tools</code> README</a> defines each extension point&#39;s inputs, order, return values, and failure behavior.</p><h2 id="code-mode-reaches-your-tool-for-free" tabindex="-1">Code Mode reaches your tool for free <a class="header-anchor" href="#code-mode-reaches-your-tool-for-free" aria-label="Permalink to &quot;Code Mode reaches your tool for free&quot;">​</a></h2><p>In <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/core/tools/README.md" target="_blank" rel="noreferrer">Code Mode</a>, every visible registered tool is available as <code>await tools.&lt;name&gt;(args)</code> without extra integration. The generated <code>ToolArgsMap</code> and <code>ToolOutputMap</code> derive exact argument and canonical-return types from the same schemas, and calls re-enter the normal execution pipeline. A successful call resolves to the final canonical JSON value after policy, not to rendered Native content. A failed call rejects with the real <code>ToolCallError</code>; programs can inspect only its <code>name</code>, <code>toolName</code>, and human-readable <code>message</code>, not internal error codes or a failure union.</p><p>Design <code>output.schema</code> as a useful programmatic API: return handles and fields directly, allow scalar/array/null roots when they are the honest value, and keep human explanation in <code>output.render</code>. Intermediate values are execution-local, are not persisted or prompt-truncated, and have no byte cap, so the producer&#39;s truthful acquisition bounds and process memory still matter. Only the outer <code>run_code</code> logs/result cross the configurable output cap and model-facing spill pipeline.</p><h2 id="how-your-tool-renders-in-a-ui" tabindex="-1">How your tool renders in a UI <a class="header-anchor" href="#how-your-tool-renders-in-a-ui" aria-label="Permalink to &quot;How your tool renders in a UI&quot;">​</a></h2><p>Your tool&#39;s <code>output.render</code> returns model-facing content; its <strong>UI card</strong> is a separate concern declared through pure presentation projections and optional <code>presentCall</code> / <code>presentResult</code> methods. Design these alongside the canonical value. A tool with no UI presentation falls back to a generic card (title = tool name, raw args as input).</p><p>Both methods return a <strong><code>card</code>-tagged render intent</strong> — pick the card kind that matches what your tool does:</p><ul><li><code>presentCall(args)</code> → a <code>ToolCallView</code> (the PENDING card): <ul><li><code>{ card: &#39;generic&#39;, title, kind?, rawInput?, content?, locations? }</code> — the default. Set <code>kind</code> for an icon (<code>read</code>/<code>search</code>/…); set <code>locations: [{ path, line? }]</code> for any file your tool touches so a capable editor follows along / jumps to it.</li><li><code>{ card: &#39;terminal&#39;, title, description?, cwd? }</code> — your call IS a shell command. <code>title</code> is the command, <code>description</code> renders above the terminal card. (tool-bash.)</li><li><code>{ card: &#39;diff&#39;, title, diffs, locations? }</code> — your call creates or modifies a file. <code>diffs: [{ path, oldText, newText }]</code> (<code>oldText: null</code> for a new file) renders as an inline diff card. (tool-fs <code>write</code>/<code>edit</code>.)</li></ul></li><li><code>presentResult(args, { content, isError, meta? })</code> returns the completed card: <ul><li><code>generic</code> supplies an optional title and content.</li><li><code>terminal</code> supplies raw output and optional exit metadata; each UI renders its capable or fallback view.</li><li><code>diff</code> supplies applied hunks, often derived by <code>output.presentationMeta</code> and carried in persisted <code>result.meta</code> so replay reproduces them. Mutation tools keep a diff result because the completed view replaces the pending card.</li><li><code>search</code> supplies a discovery result reconstructed from persisted <code>result.meta</code>: grouped-by-file matches (<code>shape: &#39;matches&#39;</code>, grep) or a flat path list (<code>shape: &#39;paths&#39;</code>, glob), plus <code>truncated</code>/<code>total</code> so a UI never presents a capped result as complete. The view carries no result text (a UI without a search card falls back to the raw result content), and there is no <code>search</code> call view — a discovery call&#39;s pending state stays a generic card, since matches exist only after <code>execute</code>. (tool-fs-search <code>grep</code>/<code>glob</code>.)</li><li><code>web</code> supplies a completed web retrieval, discriminated by <code>kind: &#39;search&#39; | &#39;fetch&#39;</code> (the structured search sources or the fetch summary), derived from <code>result.meta</code>; it carries no body copy, so a UI without the <code>web</code> capability falls back to the raw result content. (tool-web <code>web_search</code>/<code>web_fetch</code>.)</li></ul></li></ul><p>Hard rules (they bite if broken):</p><ul><li><strong>Purity.</strong> These run on live streaming AND on session-log REPLAY, so they must be pure functions of <code>args</code> (+ the result) — NO I/O, NO reading session state, NO clock/random. A diff is derived from the args (<code>write</code> uses <code>oldText: null</code> because a call-time presenter has no prior file content); the UI adapter, not the tool, supplies session context. If you find yourself wanting the file&#39;s old content or the working directory inside <code>presentCall</code>, stop — that belongs in durable result metadata or the adapter, not the presenter.</li><li><strong>UI-only formatting stays out of the model result.</strong> A fenced <code>\`\`\`console</code> block, a diff, a relativized path—none of these belongs in the canonical value or Native content merely to serve a UI. <code>output.render</code> owns model-facing prose; <code>presentationMeta</code> plus the card presenters own replayable UI state. A <code>terminal</code> result view carries raw output and the adapter adds any fallback framing.</li><li><strong><code>defineTool</code> soft-validates the display path.</strong> Malformed or older logged arguments make the wrapper return <code>undefined</code> (a generic fallback) rather than throw — display must never crash a replay.</li></ul><p>The neutral vocabulary lives in <code>dsh-tools</code>; tools never import a UI or transport type. Host/client runtimes map each <code>card</code> into their own view. The design and the why are in <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/architecture/2026-07-02-tool-render-intent-union.md" target="_blank" rel="noreferrer">the render-intent-union Agent Note</a>; <code>dsh-tool-fs</code> (generic/diff) and <code>dsh-tool-bash</code> (terminal) are the reference implementations.</p><h2 id="verification" tabindex="-1">Verification <a class="header-anchor" href="#verification" aria-label="Permalink to &quot;Verification&quot;">​</a></h2><p>Follow the <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/testing.md" target="_blank" rel="noreferrer">repository testing policy</a> and the owning package&#39;s test documentation. A shipped model- or UI-visible change requires the assembled coverage specified there.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/reference/cookbook/adding-a-tool.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const addingATool = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  addingATool as default
};
