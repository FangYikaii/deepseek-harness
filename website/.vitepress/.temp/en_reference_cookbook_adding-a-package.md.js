import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Cookbook: adding a workspace package","description":"","frontmatter":{"editSource":"docs/cookbook/adding-a-package.md"},"headers":[],"relativePath":"en/reference/cookbook/adding-a-package.md","filePath":"en/reference/cookbook/adding-a-package.md"}');
const _sfc_main = { name: "en/reference/cookbook/adding-a-package.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="cookbook-adding-a-workspace-package" tabindex="-1">Cookbook: adding a workspace package <a class="header-anchor" href="#cookbook-adding-a-workspace-package" aria-label="Permalink to &quot;Cookbook: adding a workspace package&quot;">​</a></h1><p>The file-by-file checklist for a new <code>@deepseek-ai/dsh-&lt;name&gt;</code> package. This checklist is validated against the bash and adapter packages as templates; if it drifts from them, fix it here.</p><h2 id="_1-create-the-package" tabindex="-1">1. Create the package <a class="header-anchor" href="#_1-create-the-package" aria-label="Permalink to &quot;1. Create the package&quot;">​</a></h2><div class="language- vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>packages/&lt;group&gt;/&lt;pkg&gt;/</span></span>
<span class="line"><span>  package.json     # copy from packages/core/tools, adjust name/description/deps</span></span>
<span class="line"><span>  tsconfig.json    # extends ../../../tsconfig.base.json, rootDir src,</span></span>
<span class="line"><span>                   # outDir lib/types, references: ../../../vendor/cosmokit,</span></span>
<span class="line"><span>                   # ../../../vendor/cordis (+ ../../../vendor/schemastery if</span></span>
<span class="line"><span>                   # you use Config, + ../../&lt;group&gt;/&lt;dep&gt; for each dsh dep)</span></span>
<span class="line"><span>  src/index.ts     # service default export or plugin (name/inject/apply/Config)</span></span>
<span class="line"><span>  README.md        # service API, events, extension points, design notes,</span></span>
<span class="line"><span>                   # + gated Model Experience context blocks or short form</span></span>
<span class="line"><span>                   # + the gated &quot;Known Limitations and Deferred Work&quot; section</span></span>
<span class="line"><span>                   # (or a whitelist entry in scripts/verify-package-readme-limitations.ts)</span></span></code></pre></div><p>Choose an existing group when one matches the package&#39;s role (<code>core</code>, <code>llm</code>, <code>bash</code>, <code>compact</code>, <code>subagent</code>, <code>todo</code>, <code>session-persistence</code>, <code>ui</code>, <code>util</code>, or <code>support</code>). A new group is allowed, but it is a pure container: no <code>package.json</code>, no source files, and packages still sit exactly one level below it.</p><p>package.json invariants (enforced by <code>pnpm run constraints</code> / <code>scripts/check-workspace-constraints.ts</code>): <code>private: true</code>, a <code>version</code> matching the root <code>package.json</code>, <code>type: module</code>, <code>main: &quot;lib/index.js&quot;</code>, <code>types: &quot;lib/types/index.d.ts&quot;</code>, <code>exports[&quot;.&quot;].types: &quot;./lib/types/index.d.ts&quot;</code>, <code>exports[&quot;.&quot;].default: &quot;./lib/index.js&quot;</code>, <code>@deepseek-ai/cordis</code> in BOTH peerDependencies and devDependencies (same range). Mirror every dsh peer dependency in devDependencies. <code>@deepseek-ai/schemastery</code> goes in <code>dependencies</code> (it is a runtime validator), matching agent-loop. The <code>files</code> list contains exactly <code>lib/index.js</code>, <code>lib/invariant.js</code>, <code>lib/types/**/*.d.ts</code>, and package-specific runtime artifacts recognized by the gate; a package whose runtime export points into the emitted tree also includes <code>lib/types/**/*.js</code>. Do not publish <code>src</code>, declaration maps, JS maps, or stale root declaration files. CLI app packages with a package <code>bin</code> include <code>lib/bin.js</code> immediately after <code>lib/index.js</code> in <code>files</code>.</p><p>In-package relative imports use explicit <code>.ts</code> specifiers in source (for example, <code>export * from &#39;./types.ts&#39;</code>). The compiler rewrites those to <code>.js</code> in emitted JS and leaves explicit <code>.ts</code> specifiers in declarations, which standard NodeNext/Node16 TypeScript consumers resolve to the sibling <code>.d.ts</code> files.</p><h2 id="_2-register-it-in-the-root-configs" tabindex="-1">2. Register it in the root configs <a class="header-anchor" href="#_2-register-it-in-the-root-configs" aria-label="Permalink to &quot;2. Register it in the root configs&quot;">​</a></h2><table tabindex="0"><thead><tr><th>File</th><th>Change</th></tr></thead><tbody><tr><td><code>tsconfig.base.json</code></td><td>no edit for an existing group; for a new group, add a <code>./packages/&lt;group&gt;/*/src</code> candidate to the <code>@deepseek-ai/dsh-*</code> wildcard</td></tr><tr><td><code>tsconfig.host.json</code> (Host package) or <code>tsconfig.client.json</code> (Client package)</td><td>add <code>{ &quot;path&quot;: &quot;./packages/&lt;group&gt;/&lt;pkg&gt;&quot; }</code> to <code>references</code> — an ordinary package belongs to exactly one aggregate, never both. <code>api/remotes</code> uses a repository-specific split because the Host generates a contract that the Client consumes in a later phase; new packages must not copy it (<a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/development.md#typescript-project-layout" target="_blank" rel="noreferrer">layout</a>)</td></tr><tr><td><code>knip.json</code></td><td>only if the package has entrypoints that repository discovery does not already cover</td></tr></tbody></table><p>A <code>packages/client/*</code> package additionally extends <code>tsconfig.base.client.json</code> instead of <code>tsconfig.base.json</code>, and a client plugin package declares <code>dsh.client</code> in package.json, exports <code>./client</code>, and calls the shared tsdown preset (<code>packages/client/tsdown.client.ts</code>) — see <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/packages/client/AGENTS.md" target="_blank" rel="noreferrer">packages/client/AGENTS.md</a> for the client-side contract.</p><p>Covered automatically by globs or package-manifest discovery — no edits needed: root <code>package.json</code> workspaces, <code>scripts/publint-all.ts</code>, <code>tsdown.config.ts</code>, <code>.oxlintrc.json</code>, <code>scripts/check-workspace-constraints.ts</code>.</p><h2 id="_3-decide-the-package-topology" tabindex="-1">3. Decide the package topology <a class="header-anchor" href="#_3-decide-the-package-topology" aria-label="Permalink to &quot;3. Decide the package topology&quot;">​</a></h2><p>For a swappable capability, separate Service Definition / Service Provider / Consumer roles into packages when they evolve independently (see docs/architecture.md § &quot;Capability seams&quot; — the shell trio is the template). A single-purpose plugin stays one package.</p><h3 id="name-the-role-that-exists" tabindex="-1">Name the role that exists <a class="header-anchor" href="#name-the-role-that-exists" aria-label="Permalink to &quot;Name the role that exists&quot;">​</a></h3><p>Name the stable current responsibility. Do not name the first implementation, a possible future expansion, or the Cordis base class. An interface package names the capability. An implementation package adds the mechanism, protocol, environment, or vendor that distinguishes it. Use <code>local</code> only when same-host execution is part of the contract.</p><p>Use a singular <code>ctx</code> key for one engine, runtime, policy, controller, resolver, store, or current configuration. Use a plural key for a registry or a service that owns multiple named members. The class role and key number must agree. Do not reuse one Cordis <code>Context</code> key for incompatible host and client declarations. TypeScript declaration merging sees both faces even when they use separate runtime contexts. Add the role suffix when the natural plural already belongs to another face.</p><table tabindex="0"><thead><tr><th>Word</th><th>Use it when</th><th>Do not use it when</th></tr></thead><tbody><tr><td><code>Controller</code></td><td>It accepts commands or user intent and changes one existing domain or presentation state.</td><td>It executes arbitrary work, owns a provider fleet, or only converts values for display.</td></tr><tr><td><code>Store</code></td><td>It owns one data set and mainly offers CRUD, snapshot, or subscription operations for that data.</td><td>It validates a state machine, arbitrates authority, dispatches work, or owns provider precedence. A map does not make a class a store.</td></tr><tr><td><code>Directory</code></td><td>It exposes entries and metadata for discovery or selection.</td><td>Producers register arbitrary implementations into it, or callers execute work through it.</td></tr><tr><td><code>Presenter</code></td><td>It is a pure conversion from domain values or tool arguments to render intent.</td><td>It performs I/O, subscribes, mutates state, or owns lifecycle.</td></tr><tr><td><code>Registry</code></td><td>It owns a dynamic set of named registrations, including lookup, duplicate or precedence rules, lifetime, and disposal.</td><td>Its main contract is dispatch, execution, cancellation, policy, or orchestration.</td></tr><tr><td><code>Runtime</code></td><td>It runs live work and owns dispatch, cancellation, provider coordination, or operation lifecycle across calls.</td><td>It only stores records, returns a catalog, resolves one value, or holds configuration.</td></tr><tr><td><code>Resolver</code></td><td>It computes or locates one answer from supplied inputs without owning that answer&#39;s lifecycle.</td><td>It owns a mutable collection or long-running execution.</td></tr><tr><td><code>Binder</code></td><td>It attaches one declared interface to a caller context or lifecycle and returns the bound value.</td><td>It owns the value as a collection, controls its domain state, or only converts data.</td></tr><tr><td><code>Engine</code></td><td>It implements a domain algorithm or stateful execution model.</td><td>It only selects a provider or forwards across a protocol boundary.</td></tr><tr><td><code>Policy</code></td><td>It decides what is allowed, selected, limited, or observed.</td><td>It performs the mechanism that the decision permits.</td></tr><tr><td><code>Executor</code></td><td>It runs one explicit request or resolved specification in one capability.</td><td>It owns a broad application lifecycle or provider catalog.</td></tr><tr><td><code>Gateway</code></td><td>It adapts a process, network, RPC, or API boundary.</td><td>It only registers same-process services or stores metadata.</td></tr><tr><td><code>Provider</code></td><td>It supplies one implementation of a capability definition. Add a mechanism or vendor qualifier when several can exist.</td><td>It is the capability definition, provider registry, or consumer runtime.</td></tr><tr><td><code>Backend</code></td><td>It implements replaceable lower-level persistence, transport, or execution behind a defined interface.</td><td>It is a user-facing service or one returned live-resource reference.</td></tr><tr><td><code>Handle</code></td><td>It refers to one live resource and controls or observes that resource.</td><td>It creates and manages the complete resource pool.</td></tr><tr><td><code>Config</code></td><td>It owns one resolved configuration value or one tightly bounded record and its update contract.</td><td>It stores a general collection, executes work, or exposes unrelated settings.</td></tr><tr><td><code>Service</code></td><td>It owns a cohesive domain service that no sharper role above states honestly.</td><td>The name exists only because the class extends Cordis <code>Service</code>.</td></tr></tbody></table><p>Use <code>SDK</code> only for the JSON-RPC client/server protocol used by the supported Python and TypeScript SDKs. DeepSeek Harness itself is an agent harness, not an SDK project. Use the canonical product spelling <code>Typert</code>, never <code>TypeRT</code> or <code>typeRT</code>.</p><h2 id="_4-write-the-package-readme" tabindex="-1">4. Write the package README <a class="header-anchor" href="#_4-write-the-package-readme" aria-label="Permalink to &quot;4. Write the package README&quot;">​</a></h2><p>Keep package-specific service API, config, events, extension points, and design notes first. The limitations section records durable consumer gaps and non-obvious maintainer constraints owned by this package; ordinary cleanup stays in its source TODO or Agent Note. An indirect Model Experience sentence may name the consumer that surfaces this package&#39;s contribution, but it does not restate that consumer&#39;s implementation. End a package README with this canonical sequence:</p><div class="language-markdown vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">markdown</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">## Model Experience</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">### Request context and condition</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">#### What the model sees</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">The exact data-dependent fields, an anchored generated-catalog link, or an introduction to the verbatim literal below.</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">##### Verbatim text for this field, when needed</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">\`\`\`markdown</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Stable system-prompt prose of any length, or another long non-generated literal, copied exactly from source.</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">\`\`\`</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">#### Token effect</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Fixed, conditional, retained, replaced, capped, or zero-direct token effect.</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">#### KV Cache effect</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">Append-only, prefix-stable, replacing, or independent behavior, including the exact conditions that may invalidate reuse.</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-light-font-weight": "bold", "--shiki-dark": "#79B8FF", "--shiki-dark-font-weight": "bold" })}">## Known Limitations and Deferred Work</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#E36209", "--shiki-dark": "#FFAB70" })}">-</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-light-font-weight": "bold", "--shiki-dark": "#E1E4E8", "--shiki-dark-font-weight": "bold" })}"> **Consumer-visible gap**</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> — exact missing operation or case, its consequence, and any maintainer constraint.</span></span></code></pre></div><p>Fill Model Experience from the implementation. Use one H3 per direct, conditional, capped, lifetime, or auxiliary model-context entry, with the three ordered H4 fields shown above and one prose paragraph under each. Quote stable text owned by the package: system-prompt prose goes in a titled H5 plus <code>markdown</code> fence under the field that introduces it—normally <code>What the model sees</code>—other short literals stay inline with named placeholders, and other long literals use the same nested form. Summarize only data-dependent or provider-owned text. A tool-schema entry links its anchored section in the generated <a href="./../tool-catalog">tool catalog</a> and states only deltas absent there. Keep prompt and schema entries separate when scoping can hide one without the other. In <code>KV Cache effect</code>, distinguish append-only growth, a stable repeated prefix, replacement of earlier request tokens, and an independent model request, then name the package-owned changes that can invalidate reuse. “Does not invalidate” means the package preserves an already-reusable prefix; provider cache availability and eviction remain outside the package contract. The <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/skills/dsh-prose-standard/SKILL.md" target="_blank" rel="noreferrer">prose standard</a> governs completeness and ownership; the verifier enforces the required section structure.</p><p>A package with no context effect or one consumer-owned path uses the audited <code>None, as </code> or <code>Indirectly, through </code> sentence in <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/scripts/verify-package-readme-model-experience.ts" target="_blank" rel="noreferrer"><code>SENTENCE_MODEL_EXPERIENCE</code></a>, followed by a <code>KV Cache effect</code> H4 and one non-empty paragraph; a model-agnostic generic package may instead join <code>NO_MODEL_EXPERIENCE_SECTION</code>. Do not expand either case into a description of another package&#39;s work. The limitations <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/scripts/verify-package-readme-limitations.ts" target="_blank" rel="noreferrer">allowlist</a> is independent. The <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/.agents/notes/implemented/process/2026-07-12-package-model-experience-contract.md" target="_blank" rel="noreferrer">Model Experience Agent Note</a> records the rationale.</p><h2 id="_5-verify" tabindex="-1">5. Verify <a class="header-anchor" href="#_5-verify" aria-label="Permalink to &quot;5. Verify&quot;">​</a></h2><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">        # registers the workspace</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> doc-sync</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> constraints</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> typecheck</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> lint</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> build</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> &amp;&amp; </span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> run</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> hygiene</span></span></code></pre></div><p>Follow the <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/testing.md" target="_blank" rel="noreferrer">repository testing policy</a> for the behavior-specific checks and coverage required by the new package.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/reference/cookbook/adding-a-package.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const addingAPackage = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  addingAPackage as default
};
