import { ssrRenderAttrs } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Cordis Primer","description":"","frontmatter":{"editSource":"docs/cordis-primer.md"},"headers":[],"relativePath":"en/reference/cordis-primer.md","filePath":"en/reference/cordis-primer.md"}');
const _sfc_main = { name: "en/reference/cordis-primer.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="cordis-primer" tabindex="-1">Cordis Primer <a class="header-anchor" href="#cordis-primer" aria-label="Permalink to &quot;Cordis Primer&quot;">​</a></h1><p>Cordis is the vendored plugin framework underneath DeepSeek Harness. This primer teaches the Cordis ideas a harness plugin author needs before reading the generated service/event reference on the <a href="./subsystems/core">subsystem pages</a>; the <a href="./../develop/cordis-tutorial/">Cordis tutorial</a> walks the same ideas hands-on. The vendored source and sync procedure live in <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/vendor/README.md" target="_blank" rel="noreferrer">vendor/README.md</a>.</p><h2 id="cordis-in-five-ideas" tabindex="-1">Cordis In Five Ideas <a class="header-anchor" href="#cordis-in-five-ideas" aria-label="Permalink to &quot;Cordis In Five Ideas&quot;">​</a></h2><ul><li><strong>A plugin is a object that implements Service.</strong> It can be a function with optional <code>inject</code> and <code>apply(ctx)</code> fields, or a <code>Service</code> subclass whose lifecycle Cordis mounts into the current context.</li><li><strong>A context is a repository of services.</strong> A service claims a stable <code>ctx.&lt;key&gt;</code> such as <code>ctx.tools</code>, <code>ctx.llm</code>, or <code>ctx.sessions</code> from a context; other plugins find services via key instead of importing a concrete implementation.</li><li><strong>Declare service dependency via <code>inject</code>.</strong> A plugin that names required services waits until those services exist, so load order is expressed through service requirements rather than manual boot sequencing.</li><li><strong>Typed Events for communication.</strong> Services declare event names through TypeScript declaration merging, then dispatch them as <code>emit</code>, <code>waterfall</code>, <code>parallel</code>, or <code>serial</code> depending on whether listeners observe, wrap, fan out, or run in order.</li><li><strong>Registrations are reversible effects.</strong> Prompt sections, tool schemas, adapters, providers, and listeners are installed through <code>ctx.effect()</code> or <code>ctx.on()</code> so reload and teardown unwind them predictably.</li></ul><h2 id="dispatch-modes" tabindex="-1">Dispatch Modes <a class="header-anchor" href="#dispatch-modes" aria-label="Permalink to &quot;Dispatch Modes&quot;">​</a></h2><p>Every event can have one of the following dispatch mode and can only be dispatched by these methods accordingly.</p><table tabindex="0"><thead><tr><th>Mode</th><th>Awaited?</th><th>Dispatch Order</th><th>Has Return Value?</th></tr></thead><tbody><tr><td><code>emit</code></td><td>No</td><td>listeners observe in registration order</td><td>No</td></tr><tr><td><code>waterfall</code></td><td>No</td><td>listeners observe in registration order</td><td>Yes</td></tr><tr><td><code>parallel</code></td><td>Yes</td><td>all listeners observe the event in parallel</td><td>No</td></tr><tr><td><code>serial</code></td><td>Yes</td><td>listeners observe in registration order</td><td>Yes</td></tr></tbody></table><p>The dispatch mode is part of the event&#39;s public contract. New harness events document it with an <code>@mode</code> tag so the generated catalog can check declarations against dispatch sites.</p><h2 id="cordis-waterfall-semantics" tabindex="-1">Cordis Waterfall Semantics <a class="header-anchor" href="#cordis-waterfall-semantics" aria-label="Permalink to &quot;Cordis Waterfall Semantics&quot;">​</a></h2><p><code>ctx.waterfall</code> is around-middleware. A listener receives <code>(...args, next)</code>. Call <code>next()</code> to delegate the possibly wrapped result to the next service; return without <code>next()</code> to short-circuit. Values propagate through <code>next()</code>&#39;s return value.</p><p>Cooperative listeners usually mutate a shared request or decision object and then delegate. A listener can also choose to replace the result entirely and downstream listeners will only see the result after replacement. Use <code>prepend: true</code> only when the listener must run before ordinary registrations.</p><p>For single-decision events, short-circuiting is the design. A policy listener can return without <code>next()</code> when it owns the decision, while a listener that only annotates or observes must delegate.</p><h2 id="loader-configuration" tabindex="-1">Loader Configuration <a class="header-anchor" href="#loader-configuration" aria-label="Permalink to &quot;Loader Configuration&quot;">​</a></h2><p><code>@deepseek-ai/cordis-plugin-include</code> parses <code>!!js</code> into expression nodes. Loader interpolates an entry&#39;s <code>config</code> (after declared injections activate, against that plugin context — <code>ctx.serviceName</code>) and its <code>disabled</code> field (at every mount decision, against the loader context); Include preserves nested row expressions until target activation. Other entry metadata stays literal. Use overlays when the environment selects plugins.</p><h2 id="practical-rules" tabindex="-1">Practical Rules <a class="header-anchor" href="#practical-rules" aria-label="Permalink to &quot;Practical Rules&quot;">​</a></h2><p>Encapsulate behavior into plugins: a tool pipeline event belongs to <code>ctx.tools</code>, model streaming belongs to <code>ctx.llm</code>, and live agent coordination belongs to <code>ctx.agents</code>. Prefer events for interception and policy; prefer service methods for direct capability calls.</p><p>Every registration should have a disposer, either by returning one from <code>ctx.effect()</code> or using a Cordis helper that does it for you. If teardown order matters, keep the related work in one effect so disposal unwinds in the intended sequence.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/reference/cordis-primer.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const cordisPrimer = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  cordisPrimer as default
};
