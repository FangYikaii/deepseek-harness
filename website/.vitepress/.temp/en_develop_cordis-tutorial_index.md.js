import { ssrRenderAttrs, ssrRenderStyle } from "vue/server-renderer";
import { useSSRContext } from "vue";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Cordis tutorial","description":"","frontmatter":{"editSource":"docs/cordis-tutorial/index.md"},"headers":[],"relativePath":"en/develop/cordis-tutorial/index.md","filePath":"en/develop/cordis-tutorial/index.md"}');
const _sfc_main = { name: "en/develop/cordis-tutorial/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  _push(`<div${ssrRenderAttrs(_attrs)}><h1 id="cordis-tutorial" tabindex="-1">Cordis tutorial <a class="header-anchor" href="#cordis-tutorial" aria-label="Permalink to &quot;Cordis tutorial&quot;">​</a></h1><p>Cordis is the plugin framework underneath DeepSeek Harness: a small runtime where every capability — tools, LLM adapters, file access, the agent loop itself — is a plugin mounted into a shared context. This tutorial teaches Cordis hands-on: each chapter is a runnable example you build in a scratch directory inside this repository, ending with a plugin wired into real harness services.</p><p>The audience is agent developers. You do not need deep TypeScript experience; the <a href="#typescript-notes">TypeScript notes</a> below explain the syntax that may be unfamiliar, and every chapter shows the exact commands and expected output.</p><p>If you want the condensed concept reference instead of a walkthrough, read the <a href="./../../reference/cordis-primer">Cordis primer</a>. The exhaustive API reference lives in the generated <code>cordis-surface</code> regions on the <a href="./../../reference/subsystems/core">subsystem pages</a> and the <a href="./../../reference/cordis-api/context">Cordis core API</a> pages.</p><p>To write plugins for the harness itself — loaded from a <code>cordis.yml</code> and driven from the Web UI rather than the launcher below — start from <a href="./../basic/">your first Harness plugin</a>.</p><h2 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to &quot;Setup&quot;">​</a></h2><p>You need a clone of this repository with dependencies installed; the <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/docs/development.md#setup-tutorial" target="_blank" rel="noreferrer">development guide</a> lists the prerequisites. No API key is needed for this tutorial; every example runs keylessly.</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">git</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> clone</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> https://github.com/deepseek-ai/deepseek-harness.git</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> deepseek-harness</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">pnpm</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> install</span></span></code></pre></div><p>Create the scratch directory the chapters work in. <code>tmp/</code> is gitignored, so nothing you write there touches version control:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">mkdir</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> -p</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> tmp/cordis-tutorial</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">cd</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> tmp/cordis-tutorial</span></span></code></pre></div><p>Every chapter runs the same command from this directory:</p><div class="language-sh vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">sh</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">node</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> --import</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> tsx</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> ../../vendor/cordis/bin.js</span></span></code></pre></div><p>That one-file launcher (see <a href="https://github.com/deepseek-ai/deepseek-harness/blob/master/vendor/cordis/bin.js" target="_blank" rel="noreferrer">vendor/cordis/bin.js</a>) creates a root <code>Context</code>, mounts the Loader plugin, and tells it to load <code>./cordis.yml</code> from the current directory. Everything else — which plugins exist, how they are configured — comes from that YAML file, which you will write in a moment. The <code>--import tsx</code> flag lets Node run the TypeScript files the config points at without a build step.</p><h2 id="chapters" tabindex="-1">Chapters <a class="header-anchor" href="#chapters" aria-label="Permalink to &quot;Chapters&quot;">​</a></h2><ol><li><a href="./01-first-plugin">Your first plugin</a> — a plugin is a function; the loader mounts it.</li><li><a href="./02-lifecycle-and-effects">Lifecycle and effects</a> — Cordis-managed registrations are undone when their plugin unloads.</li><li><a href="./03-services">Services</a> — expose a capability on <code>ctx</code> and depend on it with <code>inject</code>.</li><li><a href="./04-events">Events</a> — typed events, broadcast dispatch, and the waterfall short-circuit.</li><li><a href="./05-config">Configuration</a> — validated config from <code>cordis.yml</code>, failing loud on bad input.</li><li><a href="./06-composition-and-hmr">Composition and HMR</a> — the config file as a plugin tree, hot reload, and diagnosing a plugin that never loads.</li><li><a href="./07-into-the-harness">Into the harness</a> — register a model-callable tool against real harness services.</li></ol><p><a id="typescript-notes"></a></p><h2 id="typescript-notes" tabindex="-1">TypeScript notes <a class="header-anchor" href="#typescript-notes" aria-label="Permalink to &quot;TypeScript notes&quot;">​</a></h2><p>The examples use three TypeScript features beyond ordinary modern JavaScript:</p><ul><li><strong>Type annotations</strong> describe values without changing runtime behavior: <code>ctx: Context</code> says that <code>ctx</code> has the Cordis context API, <code>who: string</code> accepts text, and <code>string[]</code> means an array of strings.</li><li><strong><code>import type { Context } from &#39;@deepseek-ai/cordis&#39;</code></strong> imports only type information. It vanishes at runtime, so a plugin file that needs <code>Context</code> solely for annotations adds no runtime dependency.</li><li><strong>Declaration merging</strong> (<code>declare module &#39;@deepseek-ai/cordis&#39; { ... }</code>) adds your entries to interfaces that Cordis already declares — for example the type of a new <code>ctx.greeter</code> property or event name. It generates no runtime wiring; the plugin separately provides the service or emits the event. Chapter 3 shows the pattern in full.</li></ul><p>Chapter 5 also uses an <code>interface</code> to describe a configuration object&#39;s fields and a generic type such as <code>Schema&lt;Config&gt;</code> to say which object fields a schema validates. You can copy those declarations as shown; the surrounding text explains what each one connects.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("en/develop/cordis-tutorial/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
